package org.generation.italy.esempiCorso.sql.prime;

import org.generation.italy.esempiCorso.sql.DatabaseConnection;

import java.sql.*;

public class QueryParametrizzataBella {
    public static void main(String[] args) {
//        try(Connection connection = DatabaseConnection.getConnection()){
//            String query = "select * from books where title = ?"; // il ? è un placeholder, un segnaposto
//            PreparedStatement ps = connection.prepareStatement(query);
//            ps.setString(1, "Casper");
//            ResultSet rs = ps.executeQuery();
//            while(rs.next()){
//                int id = rs.getInt("id");
//                String title = rs.getString("title");
//                System.out.println(title.length());
//                System.out.printf("%nLibro con id: %d e titolo: %s", id, title);
//            }
//        } catch (SQLException e){
//            e.printStackTrace();
//        }
    }
}
